/**
 * Identity and access domain.
 *
 * <p>The abstract {@code User} superclass and its three specialisations, plus the
 * {@code Role} entity supplying Spring Security authorities. Mirrors the
 * {@code model.identity} package of the Task A class diagram.
 */
package lk.icbt.dentalclinic.model.identity;
