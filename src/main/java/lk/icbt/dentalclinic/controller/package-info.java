/**
 * Presentation tier.
 *
 * <p>Spring MVC @Controller and @RestController classes. This tier is responsible only for
 * HTTP concerns -- binding requests, selecting views and shaping responses. It holds no
 * business rules and never talks to a repository directly.
 */
package lk.icbt.dentalclinic.controller;
