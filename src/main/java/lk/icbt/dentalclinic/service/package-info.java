/**
 * Business logic tier.
 *
 * <p>@Service classes holding the clinic's domain rules: appointment scheduling,
 * availability checking, billing and reporting. This is the only tier permitted to
 * orchestrate repositories, and the only tier that defines transaction boundaries.
 */
package lk.icbt.dentalclinic.service;
