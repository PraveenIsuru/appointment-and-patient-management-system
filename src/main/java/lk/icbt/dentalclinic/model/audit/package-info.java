/**
 * Audit domain.
 *
 * <p>The read-only audit trail written by database triggers. Mirrors the
 * {@code model.audit} package of the Task A class diagram.
 */
package lk.icbt.dentalclinic.model.audit;
